fun fib(n) {
  print n;
  if (n <= 1) {
    return n;
  }

  return fib(n - 2) + fib(n - 1);
}

var result = fib(3);

print "result!";
print result;

The problem is that every time the function is called
it's reassigning the value of n. n apparently always points
to the same env
