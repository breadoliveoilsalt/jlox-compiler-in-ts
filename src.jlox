fun whoopie(num) {
  if (num == 0) {
    print "whoopie!";
  }
  print num;
  print num - 1;
  var newNum = num - 1;
  print newNum;
}

// UPTO here, trying to figure out
// why this fails. I need a step-through
// debugger
whoopie(10);
